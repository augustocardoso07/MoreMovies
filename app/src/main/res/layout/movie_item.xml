<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ll_movie_item"
    android:orientation="vertical"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    >

    <ImageView
        android:id="@+id/iv_movie_poster_item"
        android:layout_width="150dp"
        android:scaleType="fitXY"
        android:layout_height="203dp"
        app:srcCompat="@drawable/moonlight"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="0dp"
        android:layout_marginLeft="0dp"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginRight="0dp"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintHorizontal_bias="0.0" />

    <LinearLayout
        android:layout_width="150dp"
        android:layout_height="63dp"
        android:background="@color/colorBackgroundText"
        android:orientation="vertical"
        android:layout_marginTop="0dp"
        app:layout_constraintTop_toBottomOf="@+id/iv_movie_poster_item"
        android:layout_marginLeft="0dp"
        android:padding="4dp"
        app:layout_constraintLeft_toLeftOf="parent">

        <TextView
            android:id="@+id/tv_movie_name_item"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Nome do Filme"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_movie_release_item"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Data do Filme" />
    </LinearLayout>
</android.support.constraint.ConstraintLayout>